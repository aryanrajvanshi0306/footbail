# footbAIl PWA — Environment Variables
# Copy: cp .env.example .env

# Backend API base URL (empty = same origin via Vite proxy)
VITE_API_URL=

# For production builds pointed at real API:
# VITE_API_URL=https://api.footbail.in
